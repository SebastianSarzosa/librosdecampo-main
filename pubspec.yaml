name: libroscampo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1
  sqflite: ^2.3.3+1
  path: ^1.9.0
  supabase_flutter: ^2.8.2
  cupertino_icons: ^1.0.8
  fl_chart: ^0.70.2
  excel: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/login/
    - assets/images/usuario/
    - assets/images/inicio/
    - assets/images/proyecto/
  uses-material-design: true
